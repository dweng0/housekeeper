# VoiceAutomationService interruption orchestration

Status: ready-for-agent
Category: enhancement
Triage: Architecture clarified (ADR 0010), blockers resolved, implementation plan documented. Core behaviors done; stream lifecycle orchestration ready to build.

## What to build

Orchestrate full TTS interruption flow: mode switching on ListeningWindow, stop-word confirmation playback, yes/no response handling, stream replay.

## Acceptance criteria

- [ ] On `sendTtsStream(nodeId, chunks)` call:
  - [ ] Extract/generate streamToken from chunks or VoiceNodeHub
  - [ ] Switch ListeningWindow to "stop-word-only" mode
  - [ ] Begin TTS streaming
  - [ ] After stream completes: switch back to "normal" mode
- [ ] On stop-word utterance during TTS:
  - [ ] Cancel in-flight stream immediately
  - [ ] Play cached `__stop_confirmation__` response
  - [ ] Start 3-second listen window for yes/no intent
  - [ ] Match: "yes" / "yeah" / "yep" / "no" / "nope" / "cancel" (intent classification or keyword match)
  - [ ] On "yes": discard original stream, dispatch `unknown` intent (conversation ends)
  - [ ] On "no" or timeout: replay original stream from VoiceNodeHub cache
  - [ ] Log action: `[VoiceAutomation] Interruption: {stop-word} → {confirmation played} → {user response} → {replay|discard}`
- [ ] Stop-word confirmation not logged as conversation turn
- [ ] Integration tests: mock Pi signals, verify flow end-to-end
- [ ] Handle edge case: replay expiry (stream cache expired) → log warning, fallback to `unknown`

## Blocked by

- Issue #56 (cache variants ready)
- Issue #57 (stream caching ready)
- Issue #58 (mode switching ready)

## Notes

Yes/no classification can be simple keyword match for now (not LLM). Per ADR 0009.

## Architecture (from grill session)

Stream lifecycle orchestration documented in ADR 0010.

**Five blockers resolved:**

1. **Stream wrapper pattern** → Public `sendTtsStream(nodeId, chunks)` method on VoiceAutomationService
   - Captures token from `voiceNodeHub.sendTtsStream()`
   - Stores in `inFlightStreams` map
   - Manages mode switching (normal → stop-word-only → normal)

2. **Token preservation for replay** → Store token + fetch-on-replay approach
   - On "no": fetch chunks via `voiceNodeHub.getStreamBuffer(nodeId, token)`
   - On cache expiry: log warning, fallback to unknown intent (acceptable per spec)
   - Cache TTL ~30s per ADR 0009

3. **Context state timing** ✓ Already implemented (line 132 in current code)
   - Stop-word check runs BEFORE context check

4. **Mode switching** → Explicit setMode calls
   - Before stream: `"stop-word-only"` (suppress ambient)
   - After confirmation: `"normal"` (listen for yes/no)
   - On resolution: `"normal"` (resume ambient)

5. **Stream cancellation** → Discard token strategy
   - No explicit cancel method needed
   - Just remove from `inFlightStreams` + ignore chunks
   - Device plays naturally; system suppresses audio input via mode

## Implementation plan

**Phase 1: Wrapper method**
- Add `sendTtsStream(nodeId: string, chunks: AsyncIterable<Buffer>): Promise<string>` to VoiceAutomationService
- Implement: capture token, track in `inFlightStreams`, switch mode before/after
- Tests: verify mode switches, token tracked, completion cleans up

**Phase 2: Stop-word → replay integration**
- Wire "no" response (line 127) to call `voiceNodeHub.getStreamBuffer()` and replay
- Wire timeout (line 149) to same replay logic
- Tests: mock stop-word, verify replay fetches from cache, handles expiry

**Phase 3: Stop-word → discard integration**
- Wire "yes" response (line 120) to discard token, dispatch unknown intent
- Tests: verify token discarded, unknown intent dispatched, mode reset

**Phase 4: Integration test**
- Mock Pi signals: TTS stream in-flight, stop-word utterance, yes/no response
- Verify full flow: mode switches, confirmation plays, yes/no handled, logging correct

## Agent Brief

> *This was generated by AI during triage.*

**What's this about?**

Orchestrate end-to-end stop-word interruption: mode switching, confirmation playback, yes/no handling, stream replay. Ties together cache (#56), streaming (#57), and mode-switching (#58) into a complete flow.

**Where does it touch?**

- `src/voice/voice-automation-service.ts` — modify `onDirectedQuestion` or `onAmbientUtterance` handler
- Interaction with `ListeningWindow.setMode()` (issue #58)
- Interaction with `VoiceNodeHub.getStreamBuffer()` (issue #57)
- Interaction with `ResponseAudioCache.lookup("__stop_confirmation__")` (issue #56)

**What's already there?**

- `VoiceAutomationService` already orchestrates device-control responses
- Stream tokens will be available from `VoiceNodeHub` after issue #57
- Confirmation cache ready after issue #56
- Mode-switching available after issue #58

**How to verify?**

1. Track in-flight `sendTtsStream` calls: record `(nodeId, streamToken)` when sending
2. On `sendTtsStream()` call: set ListeningWindow mode to "stop-word-only"
3. On stop-word utterance (ambient): check if matches device-control intent
   - If yes: cancel in-flight stream, play confirmation, listen 3s
   - Match yes/no: keyword match "yes"/"yeah"/"yep" vs "no"/"nope"
   - On yes: discard stream, return `unknown` intent
   - On no/timeout: retrieve stream from cache, replay via `sendTtsStream()`
4. After TTS ends: set mode back to "normal"
5. Log: `[VoiceAutomation] Interruption: {stop-word} → {confirmation} → {response} → {action}`
6. Integration tests: mock Pi signals, verify full flow with cached streams, verify timeouts
7. Edge case: stream expired from cache → log warning, return `unknown` intent

**Acceptance:** Full interruption flow works, logging correct, integration tests pass, no regressions

