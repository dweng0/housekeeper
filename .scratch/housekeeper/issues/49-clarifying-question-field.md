# Add clarifyingQuestion field to ClassifiedIntent

Status: ready-for-agent
Category: enhancement

## What to build

Add `clarifyingQuestion` field to the `ClassifiedIntent` interface and update domain documentation to reflect the removal of timer-based context resets and introduction of low-confidence clarification flow.

This is foundational for LLM-driven clarification: when the classifier is uncertain about intent, it can return a clarifying question instead of acting on an uncertain interpretation.

## Acceptance criteria

- [ ] `ClassifiedIntent` interface includes optional `clarifyingQuestion?: string` field
- [ ] CONTEXT.md updated: remove "resets on timeout" from Conversation Context definition
- [ ] CONTEXT.md clarifies context lifecycle: opens on Directed Question response, closes on new Directed Question, resident change, or token budget exceeded
- [ ] Code compiles with no type errors

## Blocked by

None — can start immediately

## Agent Brief

> *This was generated by AI during triage.*

**What's this about?**

Foundation for LLM-driven clarification. When the Intent Classifier is uncertain (confidence < 0.7), it should return a clarifying question instead of guessing. This slice adds the field to the interface and updates domain docs.

**Where does it touch?**

- `src/ports.ts` — ClassifiedIntent interface
- `src/voice/CONTEXT.md` — remove timeout mention, document new lifecycle

**What's already there?**

- ConversationContext uses token budget (4000 chars) to bound history, no explicit timeout yet
- System prompt already has intentConfidence field; will be updated in #50
- No code currently handles clarifyingQuestion

**How to verify?**

1. Add `clarifyingQuestion?: string` to ClassifiedIntent
2. Update CONTEXT.md: remove "Resets on: timeout (configurable, default 30s)"
3. Add "Resets on: new Directed Question, Resident change, or token budget exceeded"
4. Compile: `npm run build`
5. Tests still pass: `npm test`

**Acceptance:** TypeScript compiles, CONTEXT.md updated per criteria
