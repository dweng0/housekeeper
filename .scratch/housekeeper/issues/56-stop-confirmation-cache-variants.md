# Cache stop-confirmation variants

Status: ready-for-agent
Category: enhancement

## What to build

Add `__stop_confirmation__` key to Response Audio Cache. Generate 10 natural variants of "did you want me to stop?" responses. Build variants at startup using LLM + TTS, same as device-control responses.

## Acceptance criteria

- [ ] ResponseAudioCache cache builder includes `__stop_confirmation__` key
- [ ] 10 variants generated (examples: "Did you want me to stop?", "Should I stop what I'm doing?", "Do you want me to cancel that?")
- [ ] Variants pre-rendered at startup in `data/response-cache/`
- [ ] Index updated with `{"__stop_confirmation__": {"positive": ["var1.wav", "var2.wav", ...]}}`
- [ ] Cache builder tests verify variant generation
- [ ] One variant randomly selected on lookup

## Blocked by

None — can start immediately

## Notes

Related to ADR 0009. Variants should be natural, conversational, brief (~2-3 seconds spoken).

## Agent Brief

> *This was generated by AI during triage.*

**What's this about?**

Stop-word interruption (ADR 0009) needs pre-rendered confirmation responses. When user says "wait" during device-control TTS, system plays a confirmation. Cache 10 natural variants so responses don't feel repetitive.

**Where does it touch?**

- `src/voice/response-audio-cache-builder.ts` — add `__stop_confirmation__` key generation
- `src/voice/json-response-audio-cache.ts` — lookup method (already exists, no changes needed)
- `data/response-cache/index.json` — new key added at startup

**What's already there?**

- Response cache builder already generates variants for `{deviceLabel, command}` pairs
- Existing `lookupNotFound()` method for generic responses; can follow same pattern
- LLM prompt + TTS already wired up for variant generation

**How to verify?**

1. Add `__stop_confirmation__` to cache builder logic
2. Generate 10 variants (call LLM 10x with different prompts: "Confirm you want me to stop, vary phrasing")
3. Render each via TTS
4. Verify cache index has key with 10 WAV files
5. `npm test` — cache builder tests pass
6. Spot-check: randomly select 3 variants, verify they're distinct and natural-sounding

**Acceptance:** Index updated, 10 WAVs present, randomization works, tests pass
