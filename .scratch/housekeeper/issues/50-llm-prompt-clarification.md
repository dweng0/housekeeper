# Update LLM prompt for low-confidence clarification

Status: ready-for-agent
Category: enhancement

## What to build

Modify the system prompt in `openai-intent-classifier.ts` to instruct the LLM to return a `clarifyingQuestion` instead of acting on low-confidence intents. The LLM should use `intentConfidence` to self-assess, and when confidence is below 0.7, output a clarifying question that helps the user disambiguate.

## Acceptance criteria

- [ ] System prompt updated: LLM instructed to return `clarifyingQuestion` when `intentConfidence < 0.7`
- [ ] System prompt no longer mentions `hedgedResponse` (or clarifies its new role)
- [ ] Classifier test added: low-confidence query → `clarifyingQuestion` returned in response
- [ ] Classifier test: high-confidence device-control → `response` returned, no `clarifyingQuestion`
- [ ] LLM response parsing handles optional `clarifyingQuestion` field

## Blocked by

- #49 (requires clarifyingQuestion field in ClassifiedIntent)

## Agent Brief

> *This was generated by AI during triage.*

**What's this about?**

Teach the LLM to return a clarifying question when it's unsure about user intent. Currently the code chooses `hedgedResponse` when confidence < 0.7. Now the LLM itself decides: if confidence < 0.7, return `clarifyingQuestion` instead of `response`.

**Where does it touch?**

- `src/voice/openai-intent-classifier.ts` — buildSystemPrompt function, JSON response parsing
- `src/voice/openai-intent-classifier.test.ts` — add test cases for clarifying question scenarios

**What's already there?**

- System prompt mentions `intentConfidence` (0–1), `response`, `hedgedResponse`
- LLM returns JSON-formatted ClassifiedIntent
- Confidence tests exist but only check confidence values, not clarifyingQuestion logic

**How to verify?**

1. Update buildSystemPrompt: add instruction "When intentConfidence < 0.7, return `clarifyingQuestion` instead of `response`. Clarifying question should be natural language asking the user to disambiguate."
2. Update JSON schema examples to show clarifyingQuestion in place of response for low-confidence case
3. Add test: low-confidence query → verify LLM returns clarifyingQuestion
4. Add test: high-confidence device-control → verify LLM returns response, no clarifyingQuestion
5. Run: `npm test src/voice/openai-intent-classifier.test.ts`

**Acceptance:** Tests pass, LLM returns correct field based on confidence
