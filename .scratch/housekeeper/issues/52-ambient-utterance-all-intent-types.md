# Refactor onAmbientUtterance to handle all intent types with low-confidence clarification

Status: ready-for-agent
Category: enhancement

## What to build

Extend the `onAmbientUtterance` handler in `voice-automation-service.ts` to match the full behavior of `onDirectedQuestion`. Currently it only handles `query` type follow-ups; it should now handle `device-control`, `create-automation`, `set-resident`, and `unknown` types as well.

Implement low-confidence clarification: when a follow-up intent has confidence < 0.7, speak the `clarifyingQuestion` instead of acting.

Always add turns to conversation history, even for unknown types and clarifications.

## Acceptance criteria

- [ ] `onAmbientUtterance` classifies follow-up with full conversation history
- [ ] High-confidence device-control follow-up → execute device action, speak response, add turn
- [ ] High-confidence create-automation follow-up → create automation, speak response, add turn
- [ ] High-confidence set-resident follow-up → set resident, speak response, add turn
- [ ] Low-confidence follow-up (any type) → speak `clarifyingQuestion`, add turn with clarification
- [ ] Unknown type follow-up → add to history, no response
- [ ] All turns added to context via `addTurn(utterance, spokenText)`
- [ ] Tests pass: follow-up device-control, follow-up low-confidence, follow-up unknown

## Blocked by

- #49 (ClassifiedIntent has clarifyingQuestion field)
- #50 (LLM returns clarifyingQuestion on low confidence)
- #51 (ConversationContext timer removed)

## Agent Brief

> *This was generated by AI during triage.*

**What's this about?**

Currently `onAmbientUtterance` (follow-up utterances without system name) only handles `query` type. This slice extends it to handle all intent types: device-control, create-automation, set-resident, unknown. Low-confidence intents → speak clarifyingQuestion. All utterances → add to conversation history.

**Where does it touch?**

- `src/voice/voice-automation-service.ts` — expand onAmbientUtterance handler
- `src/voice/voice-automation-service.test.ts` — add follow-up scenario tests

**What's already there?**

- onDirectedQuestion (lines 105–259) handles all intent types with full response logic
- onAmbientUtterance (lines 82–104) only handles query type, returns early for others
- Low-confidence check (line 128): isLowConfidence variable exists but only used in device-control
- Conversation context already tracks turns; just need to call addTurn consistently

**How to verify?**

1. Extract the intent-type-specific logic from onDirectedQuestion (device-control, create-automation, set-resident handling)
2. Adapt for follow-ups: same execution, but for low-confidence use clarifyingQuestion
3. Add low-confidence check to all paths (not just device-control)
4. Always call ctx.addTurn(text, spokenText) after processing, even for unknown (use empty string for no response)
5. Test scenarios:
   - Follow-up device-control → device action executes, response spoken, turn added
   - Follow-up low-confidence (any type) → clarifyingQuestion spoken, turn added
   - Follow-up unknown → no response, turn added to history
   - Follow-up create-automation, set-resident → executes like Directed Question
6. Run: `npm test src/voice/voice-automation-service.test.ts`

**Acceptance:** All intent types handled in follow-ups, low-confidence returns clarifyingQuestion, all turns added to context
